# Shared design assets

This folder is for assets genuinely shared across the MailMyPDF customer-facing UI.

Use it for brand marks, global textures, shared illustrations, and other reusable design-system media.

Section-specific assets belong in `/{section}/assets/`.

Workflow-specific assets belong in `/{section}/workflows/{workflow}/assets/`.
