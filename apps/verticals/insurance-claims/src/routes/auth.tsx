import { createFileRoute, useNavigate, useSearch } from '@tanstack/react-router'
import { useState } from 'react'
import { useAuth } from '@/src/lib/auth'
export const Route=createFileRoute('/auth')({component:Auth})
function Auth(){const{signIn,signUp,resetPassword,isConfigured}=useAuth();const nav=useNavigate();const search=useSearch({from:'/auth'}) as {returnTo?:string};const[mode,setMode]=useState<'signin'|'signup'|'reset'>('signin');const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setMsg('');try{const r=mode==='signin'?await signIn(email,password):mode==='signup'?await signUp(email,password):await resetPassword(email);if(r.error){setMsg(r.error);return}if(mode==='reset'){setMsg('Password reset instructions sent.');return}if(mode==='signup'&&r.needsConfirmation){setMsg('Check your email to confirm your account.');return}await nav({to:(search?.returnTo||'/dashboard') as never})}finally{setBusy(false)}}return <main className="container section"><div className="card auth"><div className="eyebrow">MailMyPDF Account</div><h1>{mode==='signin'?'Welcome back.':mode==='signup'?'Create your account.':'Reset your password.'}</h1>{!isConfigured&&<p className="error">Authentication is not configured.</p>}{msg&&<p className="muted">{msg}</p>}<form onSubmit={submit}><label>Email<input className="input" type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label>{mode!=='reset'&&<label>Password<input className="input" type="password" required value={password} onChange={e=>setPassword(e.target.value)}/></label>}<button className="btn" disabled={busy}>{busy?'Please wait…':mode==='signin'?'Sign in':mode==='signup'?'Create account':'Send reset link'}</button></form><div className="auth-links"><button onClick={()=>setMode('signin')}>Sign in</button><button onClick={()=>setMode('signup')}>Create account</button><button onClick={()=>setMode('reset')}>Forgot password?</button></div></div></main>}
