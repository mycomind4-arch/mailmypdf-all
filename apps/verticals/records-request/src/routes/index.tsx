import { createFileRoute, Link } from '@tanstack/react-router'
import { recordsWorkflows } from '../workflows'
export const Route=createFileRoute('/')({component:Home})
function Home(){const featured=recordsWorkflows.slice(0,6);return <main className="shell"><div className="hero"><div className="eyebrow">RECORDS REQUESTS</div><h1>Find and prepare the records request that matches your objective.</h1><p className="lead">Explore focused public-records workflows, complete a workflow-specific request builder, and keep the request tied to your account.</p><div className="actions"><Link className="btn primary" to="/workflows">Explore all workflows →</Link><Link className="btn" to="/auth">Sign in</Link></div></div><section><div className="eyebrow">FEATURED</div><div className="workflow-grid">{featured.map(w=><Link key={w.id} className="workflow-card" to="/workflows/$slug" params={{slug:w.id}}><h2>{w.name}</h2><p className="muted">{w.description}</p><span className="arrow">Open workflow →</span></Link>)}</div></section></main>}
