import { createFileRoute, Link } from '@tanstack/react-router'
import { useAuth } from '../../lib/auth'
const label=(s:string)=>s.split('-').map(x=>x.charAt(0).toUpperCase()+x.slice(1)).join(' ')
export const Route=createFileRoute('/workflows/$workflowId')({component:Detail})
function Detail(){const{workflowId}=Route.useParams();const{user}=useAuth();return <main className="shell"><Link className="back" to="/workflows">← All workflows</Link><div className="hero compact"><div className="eyebrow">CODE ENFORCEMENT WORKFLOW</div><h1>{label(workflowId)}</h1><p className="lead">Prepare a structured response, organize supporting evidence, and preserve a workflow-specific record.</p><div className="actions"><Link className="btn primary" to="/workflows/$workflowId/start" params={{workflowId}}>Start this workflow →</Link>{!user&&<span className="muted">Account required before execution.</span>}</div></div><div className="grid"><div className="card"><h2>Typical inputs</h2><p className="muted">Notice or citation, relevant property records, photographs, correspondence, permits, inspection reports, and other evidence that supports your position.</p></div><div className="card"><h2>Workflow safeguards</h2><p className="muted">Execution is designed around secure intake, evidence checks, validation, human review, authorization, submission, tracking, and proof.</p></div></div></main>}
