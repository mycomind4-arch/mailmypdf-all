import { createFileRoute, Link } from '@tanstack/react-router'
export const Route=createFileRoute('/')({component:Home})
function Home(){return <main className="shell"><div className="hero"><div className="eyebrow">CODE ENFORCEMENT</div><h1>Respond to code enforcement notices with a structured workflow.</h1><p className="lead">Review the notice, organize your evidence, prepare a response, and keep a clear record of what you submitted.</p><div className="actions"><Link className="btn primary" to="/workflows">Browse workflows →</Link><Link className="btn" to="/auth">Sign in</Link></div></div><div className="grid"><div className="card"><h2>Evidence first</h2><p className="muted">The existing 14-stage Gold workflow remains in the codebase, including provenance and human-review gates.</p></div><div className="card"><h2>Workflow-specific</h2><p className="muted">Every start URL carries its workflow identity instead of collapsing users into a generic start or dashboard loop.</p></div><div className="card"><h2>Private execution</h2><p className="muted">Public workflow discovery stays open; starting analysis requires an authenticated account.</p></div></div></main>}
